<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Fila superior: ícono izquierda + botones editar/eliminar derecha -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- HU 6 Criterio 4: ícono control gamer en parte superior izquierda -->
            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/sports"
                android:contentDescription="Ícono reto"
                app:tint="@color/orange" />

            <Space
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <!-- HU 6 Criterio 4 y 7: botón editar verde con animación touch (ripple) -->
            <ImageButton
                android:id="@+id/btnEditChallenge"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/ripple_orange"
                android:src="@drawable/ic_edit"
                android:contentDescription="Editar reto"
                android:padding="6dp" />

            <!-- HU 6 Criterio 4 y 7: botón eliminar rojo con animación touch (ripple) -->
            <ImageButton
                android:id="@+id/btnDeleteChallenge"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/ripple_orange"
                android:src="@drawable/ic_delete"
                android:contentDescription="Eliminar reto"
                android:padding="6dp"
                android:layout_marginStart="4dp" />

        </LinearLayout>

        <!-- HU 6 Criterio 4: línea horizontal separadora negra -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginVertical="8dp"
            android:background="@color/black" />

        <!-- HU 6 Criterio 4: descripción del reto — texto negro sobre fondo blanco -->
        <TextView
            android:id="@+id/tvChallengeDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:textSize="14sp"
            android:lineSpacingExtra="2dp"
            android:text="Descripción del reto" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
